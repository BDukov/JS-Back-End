const sum = (a, b) => a + b;

const multiply = function(a, b) {
    return a * b;
};

const subtract = (a, b) => a - b;

const divide = (a, b) => a / b;

module.exports = {
    sum: sum,
    multiply: multiply,
    subtract: subtract,
    divide: divide
};
